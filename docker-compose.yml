version: "3.9"
services:
  api:
    image: ucb-api
    build: .
    ports:
      - "8000:8000"
    command: "uvicorn app.main:app --host 0.0.0.0 --port 8000"
  postgresql:
    image: postgres
    ports:
      - "5432:5432"
  minio:
    image: minio/minio
    ports:
      - "9000:9000"
